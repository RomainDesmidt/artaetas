<%= simple_form_for(annonce) do |f| %>
  <!-- [...] -->
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <!-- [...] -->
<% end %>
